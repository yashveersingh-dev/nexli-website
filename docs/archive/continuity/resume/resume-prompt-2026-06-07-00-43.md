# NEXLI — Resume Prompt · 2026-06-07 00:43

> Paste into a fresh Claude Code session to continue building NEXLI. Handoff written for a senior engineer.

You are taking over **NEXLI**, a premium, mobile-first **School Operating System** for Indian K-12 schools (multi-tenant SaaS; platform Super Admin above isolated per-school tenants). Quality bar: Apple-Enterprise / Linear / Stripe; **Obsidian + Gold + Ivory**; flawless mobile-first (320→1920, no horizontal scroll, 60fps on old Android); WCAG 2.1 AA. Build is **approved and underway** in `Web/`.

## Read first (in order)
1. `...\Nexli\NEXLI_MASTER_SPECIFICATION.md` (business truth)
2. `...\Nexli\NEXLI_BUILD_PLAN.md` (architecture; note §13A AI strategy, §14A free-tier constraints, §20 build sequence, §22 continuity)
3. `...\Nexli\FIREBASE_SETUP.md` (§0 ADDENDUM = confirmed free-tier mode)
4. Newest `...\Nexli\Web\context\context-*.md` (current state — this checkpoint)
5. `...\Nexli\reference\` (visual truth; already ported to `Web/src/styles/nexli.css`)

## Locked decisions (do not relitigate)
React 19 + TS + Vite installable PWA (Capacitor later). Tailwind v4 + NEXLI tokens + ported `nexli.css` + `app.css`. Firebase `nexli-erp`, **Spark/free, no Blaze, no Cloud Functions yet** (clean seams + local Admin scripts later). **RBAC via protected Firestore profile docs + security rules `get()`** (custom claims later). Auth: email/pass (staff) + phone OTP (parents, no password); no SMS MFA. Students/parents can't change own password (only `user.password.manage` roles). **Major forms = dedicated routed pages** (modals only confirm/preview/warn/simple). Payments: no gateway (school QR+bank, parents pay offline, staff record). Notifications: in-app only + info cards for future channels. Images: ImageKit (profile + QR) URLs, initials fallback until keys provided. Maps: Leaflet+OSM + driver-PWA geolocation. **AI: build every surface fully but no provider — `AILockedOverlay` with the exact "coming soon" message, gated by `aiEnabled`.**

## Working agreement
Momentum + ownership; only stop for major product/business/arch/security calls. No MVP/placeholders. Don't test-mode early. Keep continuity files fresh (`Web/context` + `Web/resume`, timestamped, newest = truth) each milestone/turn. Subagents allowed for independent modules once UI-kit conventions are set.

## Where things stand — P0 ~60%, builds GREEN
Done: scaffold (Vite/TS/PWA), Tailwind theme + nexli.css + app.css, `lib/firebase.ts` (offline), `lib/{cn,format,hooks}.ts`, `types/roles.ts`, and the **UI Kit** in `src/components/`: Icon (~50), Avatar, Badge, Button, Panel, KPICard (count-up), feedback (Skeleton/EmptyState/Spinner/InfoCard), AILockedOverlay, charts (Donut/DonutLegend/Ring/LineChart/BarChart), barrel `index.ts`. `FoundationPage` exercises the kit. Tooling: Node 22, npm 10, firebase-tools 15, git.

## Continue from here
1. `cd Web && npm install` (if needed), `npm run dev` to view; `npm run build` must stay green.
2. **Finish P0:** (a) `src/app/nav.ts` navigation manifest (role→menu, module/flag-filtered, AI-tagged); (b) **AppShell** — Sidebar + mobile Drawer + BottomNav (safe-area) + TopBar + ContextSwitcher (reference-accurate, mobile-first); (c) **DataTable** (table ≥768 / stacked cards <768) + Pager/Filters; (d) form controls (Input/Select/Textarea/DatePicker/FileUpload/OTP/Toggle/Checkbox/Radio) + Form (RHF+Zod) for dedicated-page forms; (e) Modal (small-scope), Toast provider, Sheet, Tabs; (f) i18n scaffold (i18next+en) + AppProviders.
3. **P1:** Firebase auth (email/pass + phone OTP), Firestore data layer + hooks, **security rules + indexes**, RBAC via profile docs, secondary-app provisioning, local Super Admin bootstrap script.
4. Then P2→P9 per `NEXLI_BUILD_PLAN.md` §20. Keep task list + continuity updated.

## Still needed from user (non-blocking)
ImageKit URL endpoint + public key (+ free signing endpoint); later reCAPTCHA key (App Check); `firebase login` by owner to deploy rules/indexes.
