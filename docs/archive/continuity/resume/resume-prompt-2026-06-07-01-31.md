# NEXLI — Resume Prompt · 2026-06-07 01:31

> Paste into a fresh Claude Code session to continue. Senior-engineer handoff. (Rolling history kept; newest.)

Building **NEXLI** — premium mobile-first **School Operating System** for Indian K-12 (multi-tenant SaaS; Super Admin platform above isolated school tenants). Bar: Apple-Enterprise/Linear/Stripe; **Obsidian+Gold+Ivory**; flawless mobile-first (320→1920, no horizontal scroll, 60fps on old Android); WCAG 2.1 AA. Approved & underway in `Web/`.

## Read first (in order)
1. `...\Nexli\NEXLI_MASTER_SPECIFICATION.md`
2. `...\Nexli\NEXLI_BUILD_PLAN.md` (§13A AI, §14A free-tier, §20 sequence, §22 continuity)
3. `...\Nexli\FIREBASE_SETUP.md` (§0 free-tier mode)
4. `...\Nexli\Web\docs\CONVENTIONS.md` (build contract) + `...\Web\docs\QUALITY_REVIEW.md` (completion gate)
5. `...\Nexli\Web\context\MODULE_STATUS.md` (living tracker) + newest `...\Web\context\context-*.md`
6. `...\Nexli\reference\` (visual truth → ported to `Web/src/styles/nexli.css`)

## Locked decisions
React 19+TS+Vite installable PWA (Capacitor later). Tailwind v4 + NEXLI tokens + nexli.css + app.css. Firebase `nexli-erp` **Spark/free, no Blaze/Functions yet**. Day-one: **multi-tenant isolation** (lib/db.ts + rules), **feature flags** (lib/featureFlags.ts), **audit** (lib/audit.ts). RBAC via Firestore profile docs (lib/rbac.ts). Auth: email/pass (staff) + phone OTP (parents, no password); no SMS MFA. Students/parents can't change own password (`user.password.manage` roles only). **Major forms = dedicated routed pages** (modals only confirm/preview/warn/simple). Payments: manual QR+bank. Notifications: in-app + info cards. Images: ImageKit (profile+QR) URLs, initials fallback until keys. Maps: Leaflet+OSM + driver-PWA geolocation. **AI: build all surfaces, no provider** → `AILockedOverlay` (exact msg) gated by `useFlag('ai')`.

## Working agreement
Momentum + ownership; only stop for major product/business/arch/security calls. No MVP/placeholders, don't test-mode early. **Subagents authorized** for module fan-out after foundation (integrate to keep build green). **Rolling** checkpoints (never delete). Mobile parity hard rule. **Module completion gate:** pass `QUALITY_REVIEW.md` + update `MODULE_STATUS.md` before marking complete — "functions" ≠ "done". UI quality > screen count; compare each module to reference; one-team consistency; monitor perf for old Android; validate at 320/360/375/390/412/768/1024/1440/1920.

## Where things stand — P0 ~90%, builds GREEN
Done: scaffold (Vite/TS/PWA), Tailwind theme + nexli.css + app.css, **UI Kit** (`@/components`: Icon, Avatar, Badge, Button, Panel, KPICard, feedback, AILockedOverlay, charts, **DataTable** [table→cards, states, Pager]), **spine** (types/models, lib/rbac|featureFlags|db|audit, SessionProvider → useSession/useCan/useFlag/useAudit), **nav manifest** (app/nav.ts), **AppShell** (app/shell), `ShellPreview` (principal demo at `*`), quality system (docs/QUALITY_REVIEW.md) + tracker (context/MODULE_STATUS.md). Tooling: Node 22, npm 10, firebase-tools 15, git. View: `cd Web && npm run dev`.

## Continue from here
1. Keep `npm run build` green. Finish P0: **form kit** (`src/components/form/`: Field, Input, Textarea, Select, DatePicker, Toggle, Checkbox, Radio, FileUpload[ImageKit seam], OTPInput, `Form` RHF+Zod, `FormPage` dedicated-page layout), **Modal**(small)/**Toast**(`useToast`)/**Sheet**/**Tabs**, **i18n** (i18next+en) + `AppProviders`.
2. **P1 auth**: login (email/pass + phone OTP), route guards, session-driven AppShell container (replace ShellPreview), secondary-app provisioning, local Super Admin + first-school bootstrap (`firebase login`/ADC), deploy `firestore.rules` + `firestore.indexes.json` (tenant isolation + medical/counseling/POCSO protection + append-only audit).
3. **Fan out modules** (subagents, CONVENTIONS) under `src/features/<module>/`; integrator registers routes/nav; pass each through QUALITY_REVIEW before Complete; update MODULE_STATUS + checkpoints. Order: P2 Super Admin → P3 SIS/structure/HR → P4 daily drivers+dashboards → P5 finance → P6 ops/safety → P7 compliance → P8 analytics/AI → P9 hardening.

## Still needed from user (non-blocking)
ImageKit URL endpoint + public key (+ free signing endpoint); later reCAPTCHA key (App Check); `firebase login` by owner to deploy rules + seed first Super Admin/school.
