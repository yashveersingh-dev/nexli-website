# NEXLI — Resume Prompt · 2026-06-07 01:00

> Paste into a fresh Claude Code session to continue. Handoff for a senior engineer. (Rolling history kept; this is the newest.)

You are building **NEXLI**, a premium mobile-first **School Operating System** for Indian K-12 schools (multi-tenant SaaS; Super Admin platform layer above isolated per-school tenants). Bar: Apple-Enterprise / Linear / Stripe; **Obsidian + Gold + Ivory**; flawless mobile-first (320→1920, no horizontal scroll, 60fps on old Android); WCAG 2.1 AA. Approved & underway in `Web/`.

## Read first
1. `...\Nexli\NEXLI_MASTER_SPECIFICATION.md` (business truth)
2. `...\Nexli\NEXLI_BUILD_PLAN.md` (§13A AI strategy, §14A free-tier constraints, §20 sequence, §22 continuity)
3. `...\Nexli\FIREBASE_SETUP.md` (§0 free-tier mode)
4. `...\Nexli\Web\docs\CONVENTIONS.md` (the build contract — spine APIs, kit, module layout, DOD)
5. Newest `...\Nexli\Web\context\context-*.md`
6. `...\Nexli\reference\` (visual truth; ported to `Web/src/styles/nexli.css`)

## Locked decisions
React 19+TS+Vite installable PWA (Capacitor later). Tailwind v4 + NEXLI tokens + nexli.css + app.css. Firebase `nexli-erp` **Spark/free, no Blaze, no Functions yet**. **Multi-tenant isolation** (structural via `lib/db.ts` + rules). **Centralized feature flags** (`lib/featureFlags.ts`). **Day-one audit logging** (`lib/audit.ts`). RBAC via Firestore profile docs (`lib/rbac.ts` mirrors rules). Auth: email/pass (staff) + phone OTP (parents, no password); no SMS MFA. Students/parents can't change own password (only `user.password.manage` roles). **Major forms = dedicated routed pages** (modals only confirm/preview/warn/simple). Payments: manual QR+bank (no gateway). Notifications: in-app only + info cards for future channels. Images: ImageKit (profile + QR) URLs, initials fallback until keys. Maps: Leaflet+OSM + driver-PWA geolocation. **AI: build every surface fully but provider-less** — `AILockedOverlay` (exact message), gated by `useFlag('ai')`.

## Working agreement
Momentum + ownership; only stop for major product/business/arch/security calls. No MVP/placeholders. Don't test-mode early. **Subagents authorized** for module fan-out after foundation; integrate so build stays green. Keep **rolling** continuity checkpoints (never delete old) each milestone/turn. Mobile parity is a hard rule.

## Where things stand — P0 ~70%, builds GREEN
Done: scaffold (Vite/TS/PWA), Tailwind theme + nexli.css + app.css, **UI Kit** (Icon, Avatar, Badge, Button, Panel, KPICard, feedback, AILockedOverlay, charts), lib utils (cn/format/hooks), and the **platform spine**: `types/models.ts`, `lib/rbac.ts`, `lib/featureFlags.ts`, `lib/db.ts` (tenant-scoped refs + `useDocument`/`useCollection`), `lib/audit.ts`, `app/providers/SessionProvider.tsx` (`useSession`/`useCan`/`useFlag`/`useAudit`), wired in `main.tsx`. vite manualChunks set. `docs/CONVENTIONS.md` written. Tooling: Node 22, npm 10, firebase-tools 15, git.

## Continue from here
1. `cd Web && npm install` (if needed); keep `npm run build` green; `npm run dev` to view.
2. **Finish P0:** nav manifest (`src/app/nav.ts`); app shells (`src/app/shell/`: AppShell, Sidebar, Drawer, BottomNav safe-area, TopBar, ContextSwitcher — mobile-first); DataTable (table ≥768 / stacked cards <768) + Pager/Filters; form controls + Form (RHF+Zod) for dedicated-page forms; Modal (small), Toast provider, Sheet, Tabs; i18n scaffold (i18next+en).
3. **P1 auth:** login pages (email/pass + phone OTP), route guards (status/role/flag), secondary-app provisioning helper, local Super Admin + first-school bootstrap script (run with `firebase login` / ADC). Deploy `firestore.rules` + indexes enforcing tenant isolation + sensitive-data rules.
4. **Fan out modules** (subagents, per CONVENTIONS) under `src/features/<module>/`; integrator registers routes/nav. Order per BUILD_PLAN §20: P2 Super Admin → P3 SIS/structure/HR → P4 daily drivers + dashboards → P5 finance → P6 ops/safety → P7 compliance → P8 analytics/AI → P9 hardening.
5. Update task list + rolling checkpoints throughout.

## Still needed from user (non-blocking)
ImageKit URL endpoint + public key (+ free signing endpoint); later reCAPTCHA key (App Check); `firebase login` by owner to deploy rules/indexes + run bootstrap.
