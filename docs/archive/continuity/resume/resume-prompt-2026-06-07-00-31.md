# NEXLI — Resume Prompt · 2026-06-07 00:31

> Paste this into a fresh Claude Code session to continue building NEXLI with full context. Written as a handoff to another senior engineer.

---

You are taking over development of **NEXLI**, a premium, mobile-first **School Operating System** for Indian K-12 schools (multi-tenant SaaS with a platform Super Admin layer above isolated per-school tenants). This is NOT a generic ERP — it must feel like Apple-Enterprise / Linear / Stripe quality, luxurious and elegant, in an **Obsidian + Gold + Ivory** identity, flawless mobile-first (320→1920px, no horizontal scroll, 60fps on 5-year-old Android), WCAG 2.1 AA.

## First, read these (in order)
1. `C:\Users\yashv\Desktop\Yashveer Singh\My-Apps\Nexli\NEXLI_MASTER_SPECIFICATION.md` — business source of truth (~50 roles, ~40 modules, §12 Super Admin, §13 import, §14 tech).
2. `...\NEXLI_BUILD_PLAN.md` — complete architecture, IA/nav, screen inventory, design/component/responsive/motion/a11y/interaction systems, data & security model, build sequence (§20), AI strategy (§13A), confirmed free-tier constraints (§14A), continuity system (§22).
3. `...\FIREBASE_SETUP.md` — backend config; §0 ADDENDUM has the confirmed free-tier operating mode.
4. The **latest** `...\Web\context\context-*.md` (newest timestamp) — current state.
5. `...\Web\reference\` is at `...\Nexli\reference\` — the 4 reference dashboards + `styles.css` (visual source of truth; already ported to `Web/src/styles/nexli.css`).

## Locked decisions (do not relitigate)
- **Stack:** React 19 + TS + Vite, installable PWA (Capacitor later). Tailwind v4 + NEXLI tokens + ported `nexli.css` component classes.
- **Free-tier first:** Firebase project `nexli-erp`, **Spark (no Blaze)**, Firestore **India region**. No Cloud Functions deployed (clean seams + local Admin scripts later).
- **RBAC:** role/`schoolId`/scope/permissions in protected Firestore profile docs (`/userIndex/{uid}`, `/schools/{id}/members/{uid}`), enforced in security rules via `get()`. Custom claims = later perf optimization. Multi-tenant isolation by `schoolId`.
- **Auth:** email/pass (staff) + phone OTP (parents, no password); no SMS MFA (keep MFA-ready).
- **Passwords:** students/parents CANNOT change own; only `user.password.manage` roles (Coordinator, CS HOD, School Admin, Principal, IT Admin).
- **Forms:** all MAJOR data entry = dedicated routed pages (never modals; modals only confirm/preview/warn/simple-update).
- **Payments:** no gateway — school QR images + bank details, parents pay offline, staff record + receipt (seam for Razorpay/Stripe/UPI later).
- **Notifications:** in-app only + announcements/notice board/parent comms; info cards for future WhatsApp/SMS/email/FCM.
- **Images:** ImageKit (profile photos + payment QR) as URLs; initials-on-gradient fallback until ImageKit keys + a free signing endpoint (Cloudflare Workers/Vercel) are provided.
- **Maps/GPS:** Leaflet + OpenStreetMap + driver-PWA geolocation → Firestore realtime (seam for Google Maps/GPS HW).
- **AI:** build EVERY AI surface fully (UI/data/workflow/permissions) but integrate NO provider/API/mock — show a premium glossy blurred `AILockedOverlay` with the exact message: *"AI features are currently in development and will be released in a future update. Please keep an eye on announcements from the Super Admin for upcoming AI capabilities."* Gate via `aiEnabled` feature flag (Super Admin panel toggles it later).

## Working agreement
- Build with **momentum and ownership**, as if you own the company. Make small/medium decisions yourself; only stop for major product/business/architectural/security decisions.
- **No MVP, no shortcuts, no placeholders.** Implement the complete vision; every module. Don't switch to testing mode early or ask the user to test partial work.
- Keep the **continuity system** updated: every ~10 min of active work / each milestone, and before ending a turn, write fresh `Web/context/context-YYYY-MM-DD-HH-MM.md` + `Web/resume/resume-prompt-YYYY-MM-DD-HH-MM.md` (newest timestamp = source of truth).
- You may use subagents (the user approved this) for independent, well-specified module work once UI-kit conventions are set; keep foundation/design coherence centralized.

## Where things stand (P0 in progress)
Foundation is scaffolded and **builds green** (`cd Web && npm run build`). Done: Vite/TS/PWA scaffold, Tailwind theme, ported `nexli.css`, `src/lib/firebase.ts` (offline persistence), `.env` (real config), accessibility base, smoke-test `FoundationPage`. Tooling present: Node 22, npm 10, firebase-tools 15, git.

## Continue from here (next actions)
1. `cd Web && npm install` (if node_modules missing), then `npm run dev` to view.
2. Build the **navigation manifest** (role → menu items, filtered by module/feature-flag, AI-tagged).
3. Build **AppShell**: Sidebar + mobile Drawer + BottomNav + TopBar (reference-accurate, mobile-first).
4. Build core **UI Kit**: Icon, Avatar (photo|initials-gradient), Panel, KPICard, Badge, Button, Skeleton, EmptyState, Toast, Modal (small-scope), AILockedOverlay; charts (Donut/Ring/LineChart/BarChart — port logic from `src/lib/legacy-charts.reference.js`); DataTable with responsive table→card behavior; form controls.
5. i18n scaffold (i18next, en) + Indian number util.
6. Then **P1**: Firebase auth (email/pass + phone OTP), Firestore data layer, **security rules + indexes**, RBAC via profile docs, secondary-app provisioning, local Super Admin bootstrap script.
7. Proceed through P2→P9 per `NEXLI_BUILD_PLAN.md` §20. Update task list + continuity files as you go.

## Still needed from the user (non-blocking)
ImageKit URL endpoint + public key (+ a free signing endpoint); later reCAPTCHA site key (App Check); `firebase login` by the project owner when deploying rules/indexes.
