# NEXLI — Checkpoint 2026-06-07 12:05

Rolling snapshot. Canonical: `context/LATEST_CONTEXT.md`. This records the P1 delta.

## P1 — Firebase Core & RBAC: COMPLETE (code). Build green (163 modules), dev serves.

### Auth
- `lib/auth.ts` — staff email/pass sign-in + reset; parent phone-OTP (invisible reCAPTCHA, send/confirm, E.164 India helpers); friendly error mapper.
- `lib/provisioning.ts` — secondary-app staff account creation (admin session preserved) + member/userIndex batch writes + status changes + temp-password gen.
- `features/auth/` — `AuthLayout` (premium split, mobile-compact header), `StaffLogin` (email/pass + inline forgot-password reset), `ParentLogin` (phone → OTPInput, resend cooldown), `auth.css`.

### Routing, guards, shell
- `app/AppRouter.tsx` — `ProtectedApp` gate (loading→Splash, unauth→/login, no_profile→NoAccess, suspended→Suspended) + `RoleRoutes` (per-audience nested `<Routes>` generated from the nav manifest, each route guarded by its nav permission/flag, module body from registry else `ModuleStub`).
- `app/AppLayout.tsx` — session-driven shell: filtered nav + bottom-nav, school/platform context chip, dynamic mobile title, account Sheet (profile + settings + sign-out) and notifications Sheet.
- `app/guards.tsx` — `RequirePermission/RequireFlag/RequireSuperAdmin/Guarded`.
- `app/moduleRegistry.tsx` + `app/registerModules.ts` — the integration seam: built modules register a lazy route subtree by audience + nav id; unregistered → polished `ModuleStub` so nav is never dead.
- `app/screens/` — `Splash`, `StatusScreen` + `NoAccess/Suspended/Forbidden/NotFound`, `Dashboard` (honest audience-aware welcome + module shortcuts), `ModuleStub`.
- `app/router.tsx` — `/login`, `/login/parent`, `/foundation`, `/kit`, `*`→ProtectedApp. `main.tsx` already wraps in `AppProviders`. `ShellPreview` retired (kept as a P4 dashboard design reference).
- CSS: `src/styles/screens.css` (page header, dashboard shortcuts, account sheet, sidebar account footer, splash, status pages) imported in `index.css`.

### Security / infra (in `Web/`)
- `firestore.rules` — deny-by-default; structural tenant isolation under `/schools/{id}`; RBAC via the member doc; **restricted-collection guard** so the generic tenant wildcard can't widen access to medical/immunization/counseling/pocso/grievances/members/settings/audit_log (Firestore ORs matching rules — important); append-only audit; platform collections super-admin only; signed-in read of `feature_flags/*` (SessionProvider needs it at boot).
- `firestore.indexes.json` (members roleId+name, audit_log actorUid+ts), `firebase.json` (firestore + hosting SPA + cache headers), `.firebaserc` (nexli-erp).
- `scripts/bootstrap.mjs` — Admin-SDK seed of first super admin + first school + principal (ADC creds; idempotent; CLI/env args). `package.json` scripts: `bootstrap`, `deploy:rules`, `deploy:hosting`, `deploy`; `firebase-admin` added as devDependency.

## Decisions
- Phone auth + reCAPTCHA chosen for parents (no passwords); staff email/pass. SMS MFA still off (paid).
- Module routing via a registry + nav manifest keeps modules independently pluggable (subagent-friendly) and lazy-loaded for old-Android performance.
- Rules are intentionally coarser than the client RBAC map for general tenant data (active-member read / staff write), with strict allowlists only on sensitive collections; per-module tightening continues as modules land.

## Owner action (non-blocking for further build)
Enable Email/Password + Phone auth in console; `npm i` (firebase-admin); `gcloud auth application-default login`; `npm run bootstrap …`; `npm run deploy:rules`.

## Next
**P2 — Super Admin platform**: dashboard, school registry + detail, add-school onboarding wizard (dedicated pages), subscription lifecycle, plans & pricing, feature-flag admin (global + per-school), platform announcements, impersonation, platform audit viewer, system health. Build on the registry seam; register under audience `platform`.
