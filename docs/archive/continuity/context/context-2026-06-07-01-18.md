# NEXLI — Context Checkpoint · 2026-06-07 01:18

> Rolling history kept (00-31, 00-43, 01-00, 01-18). Newest = source of truth.

## Status
**P0 (Foundation) ~85% — builds green** (`cd Web && npm run build`; CSS 10.5kB gz; app 14.8kB gz; firebase 130kB gz, react 60.8, router 30, vendor 11.8). App shell + navigation backbone complete. Remaining P0: DataTable (responsive), form controls + Form (RHF+Zod), Modal/Toast/Sheet/Tabs, i18n scaffold. Then P1 (auth + rules), then module fan-out.

## Completed since 01:00 — navigation + app shell
- `src/app/nav.ts` — **navigation manifest** (single source of truth): NavItem type; PLATFORM_NAV, STAFF_NAV (superset), PARENT_NAV, STUDENT_NAV; `audienceForRole`, `navForAudience`, `bottomNavForAudience`, `filterNav` (role permission + feature flag). AI-taggable.
- `src/app/shell/` — **AppShell** (desktop sidebar / mobile sticky app-bar + off-canvas drawer + bottom tab bar; Esc + scroll-lock + overlay), **Sidebar**, **BottomNav** (≤4 + More), **MobileAppBar**, barrel.
- `src/styles/app.css` — shell CSS: `.nx-shell/.nx-aside/.nx-main/.nx-content/.nx-overlay/.nx-appbar/.nx-bottomnav/.nx-navtag`, breakpoint **768px**, safe-area insets, tablet tweak (1023px). Reuses reference inner classes (`.sb-logo/.sb-nav__item/.sb-quick/.sb-foot/.sb-school`).
- `src/app/ShellPreview.tsx` — principal-style dashboard mounted in the shell (KPIs, attendance donut, fee LineChart, alerts) to validate nav + mobile at every breakpoint.
- `router.tsx` — `*` → ShellPreview (nav active states demoable), `/foundation` → kit smoke test.

## How to view
`cd Web && npm run dev` → http://localhost:5173 (shell preview). Resize to validate 320→1920.

## Architecture (unchanged + reinforced)
Spine: multi-tenant isolation (lib/db.ts + rules), feature flags (lib/featureFlags.ts), audit (lib/audit.ts), RBAC (lib/rbac.ts), SessionProvider. Free-tier/Spark. Mobile-first (768 breakpoint; bottom-nav+drawer). Forms = dedicated pages. AI built-but-locked. Subagents for module fan-out after foundation. Rolling checkpoints.

## Folder structure (Web/src) — added
```
app/ router.tsx FoundationPage.tsx ShellPreview.tsx nav.ts
  providers/SessionProvider.tsx
  shell/ AppShell Sidebar BottomNav MobileAppBar index.ts
components/ (UI kit) lib/ (spine+utils) styles/ (nexli.css, app.css) types/ (roles, models)
features/ routes/ locales/en/ (to fill)  docs/CONVENTIONS.md
```

## Known issues / TODO
- Shell not yet visually screenshot-verified across all 9 widths (designed to spec; do a screenshot QA pass + spot-fix). User can `npm run dev` to see.
- DataTable, form controls, Modal/Toast, i18n still pending (finish P0).
- Auth login UI + route guards pending (SessionProvider ready).
- Firebase chunk 130kB gz (later: split firestore/auth + route lazy-load).
- PWA icons png pending; ESLint not configured.

## Next recommended actions
1. **DataTable** (`src/components/DataTable.tsx`): real table ≥768 / **stacked cards <768** + Pager + Filters + row actions (fixes reference horizontal-scroll).
2. **Form kit**: Input/Select/Textarea/DatePicker/FileUpload(ImageKit seam)/OTP/Toggle/Checkbox/Radio + `Form` (RHF+Zod) + `FormPage` layout (dedicated-page pattern).
3. **Modal** (small-scope), **Toast** provider + `useToast`, **Sheet**, **Tabs**.
4. **i18n** scaffold (i18next + en) + `AppProviders` (wrap toast/i18n).
5. **P1 auth**: login (email/pass + parent phone-OTP) pages, route guards (status/role/flag), session-driven AppShell container, secondary-app provisioning, local Super Admin + first-school bootstrap script, deploy `firestore.rules` + indexes.
6. **Fan out modules** to subagents per CONVENTIONS (P2→P8), integrator wires routes/nav, build stays green.
