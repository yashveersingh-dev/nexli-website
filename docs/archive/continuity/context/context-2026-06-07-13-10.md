# NEXLI — Checkpoint 2026-06-07 13:10

Canonical: `context/LATEST_CONTEXT.md`. This records the P2 delta.

## P2 — Super Admin platform: COMPLETE. Build green (214 modules), dev boots clean, no shortcuts/placeholders.

`src/features/platform/` — 13 modules, 49 files. Built via 1 reference module by me (schools) + 5 parallel background subagents, then integrated/reconciled/build-verified.

### Foundation (me)
- `types/models.ts`: added Plan, Subscription, PlatformSettings, PlatformAnnouncement, PlatformActivity, ImpersonationSession; extended School (admin contact, counts, modulesEnabled, deletedAt, trialEndsAt).
- `lib/db.ts`: refs for subscriptions/plans/platform_activity/impersonation_sessions/announcement doc.
- `firestore.rules`: rules for those collections (super-admin only; append-only activity; impersonation create/update only).
- `features/platform/data.ts`: schools CRUD, `applySubscriptionAction`, plans upsert/delete, announcements, settings, activity feed + `logActivity`, global/per-school flags, `usePlatformAudit`. `meta.ts`: display maps + INDIAN_STATES + DEFAULT_PLAN_TEMPLATES. `platform.css`.

### Modules (registered in `app/registerModules.ts`, audience `platform`, all lazy)
- **schools/** (me, reference): registry (search/filter/DataTable→cards), 360 detail (Tabs: overview/subscription/activity), 7-step onboarding wizard (provisions Principal via `provisioning.ts` secondary-app + writes per-school flags + creates school), `SubscriptionActionModal` (lifecycle with mandatory reason → audit + activity).
- **dashboard/** (me): command center — health KPIs, needs-attention alerts, subscription-health donut, renewals-due, activity feed, system-health InfoCard. Wired into `app/screens/Dashboard.tsx` (lazy) for super admins.
- **users/** (me): platform operator + cross-school admin directory. **support/** (me): queue shell + impersonation explainer.
- **plans/** + **subscriptions/** (subagent): pricing cards + plan create/edit FormPage + seed defaults; subscriptions overview (KPIs, MRR est, renewals).
- **settings/** (subagent): general settings + feature-flag admin (global + per-school + kill switch).
- **announcements/** (subagent): compose (type/targeting/channels, Zod) + log; in-app delivery (email/SMS = seam).
- **audit/** + **activities/** (subagent): append-only audit viewer; grouped activity timeline.
- **analytics/** + **health/** (subagent): aggregate platform analytics (no PII); system-health status board + honest metrics seam.

### Patterns established (reuse for P3+)
- Feature module = `features/<area>/<module>/routes.tsx` (default export, nested `<Routes>`) + pages + local `<module>.css` (import platform/shared css + own). Register via `registerModule(audience, navId, lazy(() => import('…/routes')))`.
- Page shell `nx-page` / `nx-page__head` / `nx-page__title` / `nx-page__sub`. Lists via DataTable (auto cards <1024) + `nx-toolbar`. Major forms via `<Form>`+`<FormPage>`+`<FormSection>`. Detail via Tabs. KV rows `nx-kv`. Timelines `nx-timeline`.
- Subagent protocol that worked: give the data contract + reference module + strict file boundaries (own folder only; don't edit shared files except — some auto-added their `registerModule` line, reconciled centrally) + existing-icon list + "typecheck only, no build". 5/5 returned typecheck-clean.

## Decisions
- Super Admin dashboard is the `index` route (not a registered nav module) — branched in `app/screens/Dashboard.tsx`.
- Coarse-but-safe: platform reads are super-admin-gated by rules; modules never read student PII (only school config + aggregates).
- Impersonation (§12.7) deferred — needs a dedicated impersonated-session context; tracked in MODULE_STATUS.

## Next: P3 — School Backbone (audience `staff`)
SIS (student master profile, admissions/enrollment, student import, TC/leaving cert), academic structure (grades/sections/subjects/houses/rooms, timetable), staff records/HRMS. Build a shared `features/school/` (or per-module) data layer first (students/staff/structure collections under `schools/{id}/…`), one reference module by me, then fan out subagents. Keep build green; per-module mobile validation; QUALITY_REVIEW before Complete.
