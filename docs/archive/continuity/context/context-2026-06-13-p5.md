# NEXLI — Checkpoint 2026-06-13 (P5 complete)

Canonical: `context/LATEST_CONTEXT.md`. This is the P5 delta (same day as P4).

## P5 — Finance (manual, free-tier): COMPLETE. Build green @344 modules; dev boots clean (HTTP 200).

### Foundation (me)
- **`types/finance.ts`** — fees (FeeHead, FeeStructure(+Item), FeeInvoice(+InvoiceLine/ConcessionLine), FeePayment, FinanceSettings), expense/procurement (Requisition, Vendor, PurchaseOrder(+POItem), GoodsReceipt(+GRNItem), Expense), payroll (SalaryStructure, PayrollRun, Payslip(+earnings/deductions)). Money = whole INR rupees.
- **`features/finance/data.ts`** — tenant CRUD + real-time hooks for all finance collections via `db.ts`; generic createIn/setIn/updateIn/removeIn with audit. **`recordPayment`** runs a Firestore `runTransaction`: increments a counter doc (`finance_counters/receipt`), writes the payment with a unique receipt number (`{prefix}-{year}-{seq}`), and updates the linked invoice's paid/due/status atomically. `payrollRunId`, `payslipId`, `examResultId`-style deterministic ids.
- **`features/finance/meta.ts`** — fee/payment/expense/PO/requisition/GRN/payroll status + category display maps, `CONCESSION_TYPE_OPTIONS`, `MONTHS`/`monthLabel`, and **India statutory helpers** `computePF` (12% of basic, ₹15k ceiling), `computeESI` (0.75% of gross ≤ ₹21k), `computePT` (generic monthly slab). Editable defaults, not mandates.
- **`features/finance/finance.css`** — shared money/ledger/receipt/payslip styles incl. `fin-amount`, `fin-summary`, `fin-itemrow`, `fin-invrow`, `fin-doc`, `fin-pay-cols`, `fin-payinfo` (QR+bank), and `@media print` (`fin-print`/`fin-noprint`). Imported by all 3 finance modules' routes.

### Modules (registered `staff` [+ parent/student for fees], lazy)
- **fees** (reference, me): hub `Tabs` at `/fees` — Overview (collection KPIs + recent payments), Student ledger (searchable dues list → `StudentLedgerPage` with assign-fees + concession modals + payment history), Structures (fee-heads modal + structure cards → `FeeStructureFormPage` RHF + `useFieldArray` line items, annualised totals), Payments (DataTable + receipt links), Settings (UPI/bank/QR + receipt prefix). Dedicated pages: `CollectPaymentPage` (`/fees/students/:id/pay` → `recordPayment` → receipt), `ReceiptPage` (`/fees/receipt/:id`, printable `ReceiptDoc` with amount-in-words). **Parent/student** `MyFeesRoutes` at `/fees`: dues/invoices/receipts per child + `PaymentInfoCard` (how-to-pay).
- **expense & procurement** (subagent): hub `/expense` — Expenses (KPIs + DataTable, `ExpenseFormPage` new/edit, approve/pay, petty cash, bill-URL seam), Requisitions (`RequisitionFormPage` line items → detail submit/approve/reject → Create PO via router state), Purchase orders (`PurchaseOrderFormPage` vendor+items+tax → detail with GRN goods-receipt modal updating PO status), Vendors (CRUD modal). `docNumber()` makes EXP-/REQ-/PO-/GRN- numbers.
- **payroll** (subagent): hub `/payroll` — Salary structures (staff list → `SalaryStructureFormPage` RHF basic/hra/da/conveyance/special + otherEarnings field-array, live gross/CTC + statutory preview, pf/esi/pt toggles, tds), Payroll runs (new-run month/year modal → generates draft payslips for staff with active structures; `RunDetailPage` finalize→mark-paid, per-payslip LOP edit), printable `PayslipDoc`/`PayslipPage` (`fin-pay-cols`, `window.print()`).

### Fixes / decisions beyond modules
- Added the **`expense` nav item** to `STAFF_NAV` (`expense.read`, icon `box`). `fees`/`payroll` already existed.
- **Finance forms pattern (important, propagated to subagents):** the kit's `Form<T>` types `schema: ZodType<T>` (input === output). `z.coerce.*` and `.default()` diverge input/output and **fail to compile**. So all finance RHF schemas are **string-based** (numeric fields as `z.string()` with a refine), `Number()`-coerced at submit, with defaults via `defaultValues`. `feeSchema.ts` is the canonical example.
- Icons: only names present in `Icon.tsx` (swapped invalid `tag`/`zap`/`wrench`/`smartphone`). Documented the safe set for subagents.

### Notes / follow-ups
- Manual reconciliation by design (no payment gateway on Spark); ImageKit QR upload still pending owner keys (seam in place).
- `firestore.rules`: P5 collections currently fall under the generic tenant wildcard (active-member read / staff write). **Payroll + salary_structures should get a tighter `payroll.*`/HR allowlist** when rules are next revised — tracked for P9 hardening.
- Both subagents returned type-clean; protocol (reference module by me + parallel subagents on the shared data layer) continues to work. Same-session P4 + P5 both landed green with checkpoints.

## Next: P6 — Operations & Safety
Transport & fleet (Leaflet/OSM live map, routes, bus attendance, SOS), hostel & residential (rollcall/exeat/night), medical & clinic (records/immunization/IHP — sensitive, restricted collection), visitor & gate (register/OTP/blacklist), canteen & nutrition (menu/FSSAI/headcount), asset & facility (register/maintenance). Several are feature-flagged (transport/hostel/medical/canteen). Build a reference (transport or visitor) then fan out. Mind the restricted collections (medical) already guarded in `firestore.rules`.
