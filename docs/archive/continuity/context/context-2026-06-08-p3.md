# NEXLI — Checkpoint 2026-06-08 (P3 complete)

Canonical: `context/LATEST_CONTEXT.md`. P3 delta.

## P3 — School Backbone: COMPLETE. Build green @249 modules; dev boots clean.

### Foundation (me)
- Types: `types/sis.ts`, `types/academics.ts`, `types/hr.ts`.
- Shared data layer `features/school/data.ts` — tenant CRUD (students/admissions/transfer_certificates/grades/sections/subjects/houses/rooms/timetable_slots/substitutions/staff/leave_requests) via `db.ts` tenant helpers; generic createIn/updateIn/removeIn with audit; `nextAdmissionNo`. `features/school/meta.ts` (status + option maps). `features/school/school.css` (shared toolbar/detail/kv/timeline/guardian/statstrip — staff routes do NOT load platform.css, so generic classes are duplicated here).

### Modules (registered `staff`, lazy)
- **students** (reference, me): registry (search + grade/section/status filters), 360 profile (Overview/Enrolment/Guardians/IDs tabs), add/edit FormPage with dynamic guardians (`useFieldArray`). Sub-routes: **/import** (CSV wizard — dependency-free parser `import/csv.ts` + `import/validate.ts` by subagent, `StudentImportPage` wizard by me: template download, upload, auto column-map, per-row validate, preview with error report, batched `createStudent`), **/tc** (TransferCertificate list / request / detail clearance-workflow → approve → issue w/ generated TC number, by me).
- **admissions** (subagent): pipeline (stage summary strip + filterable table), new-application FormPage, detail with stage stepper + document checklist + Admit (→ creates Student) / Reject / Waitlist.
- **academics** (subagent): hub Tabs — Structure (Grades/Sections/Subjects/Houses/Rooms CRUD via modals + ConfirmModal), Timetable (weekly grid ≥768px, **mobile per-day list <768px**, deterministic slot ids), Substitutions.
- **hr** (me): staff list (search + dept/status filters), 360 profile (Overview/Professional/Contact tabs), add/edit FormPage with dynamic qualifications, **/leave** (requests list + apply modal + approve/reject).

### Notes / lessons
- Session-limit interruptions hit the fan-out twice. Recovery: the cut subagents persisted NO files the 1st time (rebuilt academics+admissions via re-spawn, HR by me); the 2nd time import+TC subagent left only `csv.ts`+`validate.ts` (excellent, reused) — I built the pages directly. **Lesson: keep finishing cut modules inline rather than relying on re-spawns; commit/build after each module.**
- P3 collections need no `firestore.rules` change — covered by the generic tenant wildcard (active-member read / staff write). Sensitive collections (medical/counseling/pocso/grievances) remain separately restricted.
- Permissions gating used: `students.write` (students/admissions/import/TC), `academics.write` (academics), `hr.write`/`leave.write` (HR).

## Next: P4 — Daily Operations & Dashboards (audience `staff`, also `parent`/`student`)
Attendance (offline-first, period/daily, 75% alerts), assessment/gradebook (entry/publish/analytics), homework (assign/submit/track), examinations (schedule/admit/results), library (catalog/circulation), announcements & communication (circulars/inbox/PTM/emergency), + 4 role command-center dashboards (principal/teacher/parent/student) that replace the generic welcome dashboard per audience. Extend `features/school/data.ts` (or add `features/daily/data.ts`) with attendance/grades/homework/exam/library/communication collections; build a reference module (attendance) then fan out.
