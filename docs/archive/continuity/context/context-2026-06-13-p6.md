# NEXLI — Checkpoint 2026-06-13 (P6 complete)

Canonical: `context/LATEST_CONTEXT.md`. P6 delta (same day as P3→P4→P5).

## P6 — Operations & Safety: COMPLETE. Build green @412 modules; dev boots clean (HTTP 200).

### Foundation (me)
- **`types/ops.ts`** — transport (Vehicle, TransportRoute(+RouteStop), TransportMember, BusAttendance, SosAlert, VehiclePosition), hostel (HostelBlock, HostelRoom, HostelAllocation, HostelRollcall, ExeatPass), medical (MedicalRecord[kind:'record'], ClinicVisit[kind:'visit'], Immunization), visitor (VisitorLog, BlacklistEntry), canteen (CanteenMenu(+MealItem), MealHeadcount, CanteenFeedback, CanteenInspection), asset/facility (Asset, Facility, MaintenanceRequest).
- **`features/ops/data.ts`** — tenant CRUD + real-time hooks for all P6 collections via `db.ts`; deterministic ids (`busAttendanceId`, `rollcallId`). **Medical/clinic data lives in the rules-RESTRICTED `medical` collection** (with `kind: 'record' | 'visit'` discriminator) + `immunization` — so the existing `firestore.rules` allowlist (nurse/doctor/principal/vp_admin) already protects it; no rule change.
- **`features/ops/meta.ts`** — status/type display maps for every P6 entity (+ `BLOOD_GROUPS`).
- **`features/ops/ops.css`** — shared ops styles: `ops-map` (Leaflet), `ops-bus-marker`, `ops-sos` (pulsing alert banner), `ops-pass`/`ops-pass__otp` (gate pass), `ops-menu-day`/`ops-meal`/`ops-veg`, `ops-roster`/`ops-seg` (rollcall/bus roster), `ops-meter` (occupancy), and an `@media print` block (`fin-print`/`fin-noprint`) so ops is self-sufficient.

### New dependency
- **Leaflet** (`leaflet@^1.9.4` + `@types/leaflet`) for the transport OSM live map. Used **imperatively** (no react-leaflet → React 19 safe): map created in `useEffect`, OSM tiles, `L.divIcon` markers (avoids Leaflet's bundler-broken default marker images), `fitBounds`, cleanup on unmount. See `features/transport/components/LiveMap.tsx`.

### Modules (registered `staff`, lazy)
- **visitor** (reference, me): hub Tabs `/visitor` — Gate register (on-premises KPIs + check-out), Visitor log (DataTable), Blacklist (CRUD). `VisitorCheckInPage` (dedicated FormPage → generates pass no + 4-digit OTP, warns on blacklist match), `VisitorPassPage` (printable gate pass + OTP + check-out). nav `visitor` (`visitors.read`).
- **transport** (subagent): hub `/transport` — Live map (Leaflet/OSM + honest "no live vehicles" overlay), Routes (FormPage with stops field-array + student assignment modal), Vehicles (CRUD + fitness/insurance/PUC/permit expiry flags), Bus attendance (route+date+trip roster, offline), SOS (raise/acknowledge/resolve). Flag `transport`.
- **hostel** (subagent): hub `/hostel` — Blocks & rooms (occupancy meter), Allocations (bed-count sync), Rollcall (morning/evening/**night**, offline), Exeat passes (request→approve→out→returned + live overdue). Flag `hostel`.
- **medical** (subagent): hub `/medical` — Clinic visits (FormPage), Health records (allergies/conditions/meds chip lists + IHP), Immunizations (due/overdue). **Self-gates on `can('medical.read')`**; confidentiality notes throughout; restricted collections. Flag `medical`.
- **canteen** (subagent): hub `/canteen` — Menu (FormPage, meals→items field-array, veg/non-veg dots), Headcount, Feedback (avg rating), Hygiene & FSSAI inspections. Flag `canteen`.
- **facility** (subagent): hub `/facility` — Assets (register FormPage + warranty flags + value KPIs), Facilities (CRUD), Maintenance (tickets open→assigned→in_progress→done, `MT-` numbers). nav `facility` (`facility.read`).

### Decisions / notes
- Added nav items `visitor` + `facility`; the other four are the pre-existing flagged nav entries. The `security` nav item (CCTV/security monitoring) is intentionally left as a stub — not part of P6's core "visitor & gate" scope.
- Same protocol as P4/P5: reference module by me + parallel subagents on a pre-built shared data layer. 5 subagents this time; all returned type-clean (a transient `transport/VehiclesTab.tsx` error during the parallel window resolved by the time the transport agent finished; full `tsc` + `vite build` are green).
- Transport live positions assume a future driver-side geolocation writer (`vehicle_positions/{vehicleId}`); the map degrades honestly to stops-only when empty. No routing/ETA (free-tier).

### Follow-ups for P9 hardening
- Tighten `firestore.rules` for sensitive/role-specific P6 data (transport SOS, hostel rollcall) where useful; payroll/salary allowlist still pending too.
- A driver/conductor mobile surface (bus attendance + SOS + geolocation) could be a later PWA-role addition.

## Next: P7 — Compliance & Governance
Compliance calendar (deadlines/alerts/vault), UDISE+ reporting (auto-populate/export), RTE quota & reimbursement (EWS/lottery/claims), POCSO & grievance (CPO-only — restricted `pocso`/`grievances` collections already in rules), consent management (DPDP purpose consent/withdrawal), SMC portal (meetings/budget). Build a reference (compliance calendar or consent) then fan out; respect the `pocso`/`grievances`/`counseling` restricted collections.
