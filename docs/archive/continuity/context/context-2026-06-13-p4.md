# NEXLI — Checkpoint 2026-06-13 (P4 complete)

Canonical: `context/LATEST_CONTEXT.md`. This is the P4 delta.

## P4 — Daily Operations & Dashboards: COMPLETE. Build green; dev boots clean (HTTP 200).

### Recovery note (important)
A previous P4 session was cut off mid-integration, leaving the build **red**:
- `app/screens/Dashboard.tsx` had the 4 audience dashboards lazy-imported but never rendered (4× TS6133 unused) — the audience branch was missing.
- `features/dashboards/StudentDashboard.tsx` imported a non-existent `useTimetable` from `@/features/daily/data` (it lives in `@/features/school/data`), causing a missing-export error + cascade implicit-`any`.
- `communication`, `gradebook`, `library`, and the 3 dashboards were built but **not registered**; `homework` + `examinations` didn't exist.

Recovery actions (all done): fixed the `StudentDashboard` import; rewrote `Dashboard.tsx` as a clean lazy audience-router (platform/staff/parent/student); registered gradebook/library/communication (+ parent/student variants); built homework + examinations; full build green.

### Foundation (pre-existing from the cut session, verified good)
- `types/daily.ts` — AttendanceDay, Assessment/AssessmentResult, Homework/HomeworkSubmission, Exam/ExamPaper/ExamResult, LibraryBook/BookCirculation, Circular/PTMSlot.
- `features/daily/data.ts` — tenant CRUD + real-time hooks for attendance/assessments/homework/exams/library/circulars/ptm via `db.ts`; generic createIn/setIn/updateIn/removeIn with audit; `attendanceDayId`/`examResultId` deterministic ids; one-shot `dailyQueryOnce`.
- `features/daily/meta.ts` — status/category display maps + `ATTENDANCE_MIN_PERCENT = 75`, `BOOK_CATEGORIES`, audience/type option lists.

### Modules (all registered in `app/registerModules.ts`, lazy)
- **attendance** (staff `attendance`; parent/student `attendance` → `MyAttendanceRoutes`): MarkAttendancePage (section+date roster, per-student P/A/L/Leave segmented control, all-present, offline-tolerant save), AttendanceOverviewPage (per-student %, <75% alerts), MyAttendancePage (read view).
- **gradebook** (staff `gradebook`): AssessmentsListPage (filter/search, DataTable→cards), AssessmentFormPage (dedicated FormPage), MarksEntryPage (per-student marks + grade + remark, publish).
- **homework** (staff `homework`; parent/student `assignments` → `MyHomeworkRoutes`): HomeworkListPage, HomeworkFormPage (new/edit), HomeworkDetailPage (submission tracker over the section roster — teacher sets marks/feedback), MyHomeworkPage (upcoming/past, student "mark submitted"). Module-local `data.ts` adds two student-scoped query hooks; `homeworkSchema.ts` has `effectiveStatus`/`submissionId`.
- **examinations** (staff `examinations`; parent/student `examinations` → `MyExaminationsRoutes`): ExamsListPage, ExamFormPage, ExamDetailPage hub (Tabs) → DatesheetTab (papers CRUD via Modal), ResultsTab (grade/section marks grid → total/%/letter/pass-compartment-fail, publish), AdmitCardsTab (printable hall ticket via `window.print()`); MyExaminationsPage + ChildExams (read datesheet + report card for published exams only). `examSchema.ts` has `letterGrade`/`PASS_PERCENT`/`resultStatusFor`. **Note:** `ExamResult.marks` is keyed by **paperId** (one paper per subject), report card joins back to subject names.
- **library** (staff `library`; student `library` → `MyLibraryRoutes`): hub Tabs — CatalogTab (book CRUD, availability tracking), CirculationTab (issue/return), OverdueTab; MyLibraryPage (read + my borrowed). Gated by feature flag `library`.
- **communication** (staff `communication`; parent/student `communication` → `InboxRoutes`): CircularListPage, ComposeCircularPage (audience targeting whole-school/staff/parents/students/grade/section, category, pin, emergency), CircularDetailPage, InboxPage (audience-filtered read inbox with bottom Sheet reader).
- **dashboards** (rendered by audience in `app/screens/Dashboard.tsx`): StaffDashboard (principal/admin KPIs + teacher "my classes" branch for teaching roles + quick actions + notices), ParentDashboard (child attendance cards + upcoming homework + notices), StudentDashboard (today's classes from timetable + attendance % + homework due + notices). Platform super-admin still → PlatformDashboard.

### Fixes beyond the modules
- **Rules-of-Hooks bug** fixed in `attendance/MarkAttendancePage`, `gradebook/MarksEntryPage`, `gradebook/AssessmentsListPage`, `hr/LeaveRequestsPage`: `useCan('a') || useCan('b') || useCan('c')` short-circuited later hook calls. Replaced with `const { can } = useSession()` + plain `can('a') || can('b')`.
- **`lib/rbac.ts` `hasPermission`** enhanced with two UI-gating inferences (rules remain authoritative): (2) a scoped grant `module.action.scope` satisfies an unscoped `module.action` request; (3) a `module.write[.scope]` grant implies `module.read`. Without these, `class_teacher`/`subject_teacher` (who hold only `*.read.section`, `attendance.write.period`, `gradebook.read.section`, etc.) saw a near-empty staff nav. Now they see Students/Attendance/Gradebook/Homework appropriately; the pages still scope the data shown.

### Lessons
- Same as P3: **finish a cut-off subagent module inline; build + checkpoint after each.** The two new modules (homework, examinations) were built by parallel subagents against the already-built `daily/data.ts` layer and integrated by me — that protocol worked cleanly (both returned type-clean).

## Next: P5 — Finance (manual, free-tier)
Fee management (structure → student ledger → manual QR/bank payment record → receipt/challan, hardship/concession), expense & procurement (requisition→PO→GRN), payroll (salary structure → statutory (PF/ESI/PT/TDS) → payslips). No payment gateway (manual reconciliation; QR + bank transfer; ImageKit seam for payment-QR upload). Extend the data layer (`features/finance/data.ts`) with fee/payment/expense/payroll collections under `schools/{id}/…`; build a reference module (fees) then fan out subagents (expense, payroll). Then P6 ops/safety → P7 compliance → P8 analytics/AI → P9 hardening (incl. 9-width screenshot sweep).
