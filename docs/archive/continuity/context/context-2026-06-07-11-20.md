# NEXLI — Checkpoint 2026-06-07 11:20

Rolling snapshot. Canonical state lives in `context/LATEST_CONTEXT.md`; this records the delta for this session.

## What changed this session — **P0 Foundation completed (90% → 100%)**
Finished the remaining P0 NEXLI UI Kit and wired it into the app. `npm run build` green (148 modules, tsc clean); dev server boots and `/kit` returns 200.

### Form kit — `src/components/form/`
- **Primitives:** `Field` (a11y label/required/optional/hint/error wrapper), `Input` (left/right icons, fixed prefix, password reveal, sizes), `Textarea` (optional auto-resize), `Select` (native + custom chevron + placeholder), `DatePicker` (native date/time/month, dark color-scheme), `Toggle` (role=switch + row label/desc), `Checkbox` (real input, indeterminate), `RadioGroup` (card + inline variants), `OTPInput` (numeric, paste-fill, backspace/arrow nav, SMS one-time-code autofill), `FileUpload` (ImageKit **seam** → object-URL preview + initials avatar fallback; `uploader` prop activates real upload with no call-site change; avatar/image/file kinds, drag-drop).
- **`Form`** — RHF + Zod (`zodResolver`), `FormProvider`, `mode='onTouched'`, render-prop or children. Connected fields read errors by path: `FormInput/FormTextarea/FormSelect/FormDate` (register) and `FormToggle/FormCheckbox/FormRadioGroup/FormOTP/FormFileUpload` (Controller).
- **`FormPage`** — dedicated-page layout: back button + breadcrumbs + title/subtitle, body of `FormSection`s (responsive 2-col grid → 1-col <640px, `FormRow`/`.nx-col-full` to span), **sticky save bar** (Cancel + submit; sits above the mobile bottom-nav + safe-area). This is the canonical shell for every major data-entry form.

### Overlays & navigation — `src/components/`
- `Portal` (body overlay root), `Modal` + `ConfirmModal` (focus trap, Escape/backdrop close, scroll lock, mount/exit transition, tones, → bottom sheet <480px), `Sheet` (bottom / right / left, grab handle, safe-area), `Tabs` (line/pill, full WAI-ARIA: roving tabindex, Arrow/Home/End), `Toast` (`ToastProvider` + `useToast` with `success/error/warning/info` + action + auto-dismiss + aria-live).

### Spine / infra
- `lib/hooks.ts`: added shared overlay hooks `useEscapeKey`, `useLockBodyScroll` (ref-counted), `useFocusTrap` (focus-in + restore + Tab cycle — also satisfies the app-shell focus-trap TODO), `usePresence` (enter/exit transitions, reduced-motion aware), `useEvent`.
- **i18n:** `lib/i18n.ts` (i18next + react-i18next, en bundled inline, no Suspense) + `locales/en/common.json`. Number/date/₹ formatting stays in `lib/format.ts`.
- **`app/providers/AppProviders.tsx`** composes i18n → Session → Toast; `main.tsx` now renders `<AppProviders>` around the router.
- `Icon.tsx`: added `eye-off`, `paperclip`, `minus-circle`.
- New styles in `src/styles/kit.css` (imported after app.css). Mobile-first; inputs ≥16px on phones (no iOS focus-zoom); gold focus rings; reduced-motion respected (global rule + per-component).

### Demos / routing
- `app/KitPreview.tsx` at **`/kit`** — interactive smoke test of every primitive (standalone + connected), a real `FormPage` form with Zod validation, and all overlays/toasts/tabs.
- `FoundationPage` checklist updated (form kit / overlays / i18n now done).

## Build/verify
- `npm run build` → green. CSS 78 kB (14.6 gz); vendor chunk now ~131 kB (adds RHF/zod/i18next). Firebase chunk unchanged (~130 kB gz).
- Dev probe: `/kit` → HTTP 200, no boot errors.

## Decisions worth noting
- Form primitives are presentational and standalone-usable; RHF binding is a thin connected layer — modules can use either.
- Native `<select>`/date inputs chosen deliberately (best mobile picker + a11y for free).
- FileUpload is a true seam: previews locally + exposes the File now; supplying an `uploader` later (ImageKit) needs no consumer changes.
- 9-width screenshot QA consciously deferred to the P9 hardening sweep (not a P0 blocker; build + interactive smoke verified).

## Next (P1)
Auth (staff email/pass + parent phone-OTP via `OTPInput`), route guards (status/role/flag), session-driven AppShell container replacing ShellPreview, secondary-app provisioning, local bootstrap, deploy `firestore.rules` + indexes (needs `firebase login`). Then fan out feature modules P2→P9.
