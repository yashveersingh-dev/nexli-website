# NEXLI — Context Checkpoint · 2026-06-07 00:43

## Current implementation status
**Phase P0 (Foundation & NEXLI UI Kit) — ~60% done.** Project builds green (`cd Web && npm run build`: tsc clean; 63 modules; CSS 9.73kB gz, JS 109kB gz; PWA SW generated). Core UI-kit atoms + charts done. Remaining P0: app shells, navigation manifest, DataTable, form controls, modal/toast/sheet, i18n scaffold.

## Completed since last checkpoint (00:31)
- **UI Kit atoms** in `src/components/`:
  - `Icon.tsx` — ~50 Feather-style icons, single registry, `<Icon name size strokeWidth/>`.
  - `Avatar.tsx` — photo (ImageKit URL) or initials-on-gradient fallback.
  - `Badge.tsx` — Badge (success/warning/danger/info/muted/live) + DotBadge.
  - `Button.tsx` — variants gold/ghost/subtle/danger, sizes, loading, left/right icons.
  - `Panel.tsx` — card w/ head/body/footer-link + PanelAction.
  - `KPICard.tsx` — animated count-up (Indian/US/INR/compact/percent formats).
  - `feedback.tsx` — Skeleton, SkeletonText, Spinner, EmptyState, InfoCard (future-channel placeholder).
  - `AILockedOverlay.tsx` — glossy blurred "coming soon" veil + exact message (`AI_COMING_SOON_MESSAGE`).
  - `charts.tsx` — Donut, DonutLegend, Ring, LineChart, BarChart (SVG, reduced-motion aware).
  - `index.ts` barrel.
- **lib:** `format.ts` (Indian numbers/currency/dates/initials), `hooks.ts` (usePrefersReducedMotion, useInView, useCountUp), `cn.ts`.
- **types:** `roles.ts` — all ~50 RoleIds + tier/cluster meta + PASSWORD_MANAGER_ROLES.
- **styles:** `app.css` (NEXLI app components/mobile layer) imported after nexli.css; index.css adds the import.
- `FoundationPage.tsx` upgraded to exercise the kit (KPI count-up, Donut+legend, AI overlay, panels) — visual smoke test.

## Completed earlier (P0 base)
Vite+TS+PWA scaffold; Tailwind v4 theme mapping NEXLI tokens; nexli.css (reference verbatim); firebase.ts (offline persistence); .env (real nexli-erp config); a11y base; continuity dirs.

## In progress / next (finish P0)
1. **Navigation manifest** (`src/app/nav.ts`): role→menu items, filtered by module + feature flag, AI-tagged; powers sidebar + bottom nav.
2. **AppShell**: `Sidebar` + mobile `Drawer` (focus-trap, swipe) + `BottomNav` (5 items, safe-area) + `TopBar` (greeting, search→sheet on mobile, notifications, profile) + `ContextSwitcher`.
3. **DataTable** (`src/components/DataTable.tsx`): real table ≥768px, **stacked cards <768px** (fixes reference horizontal-scroll), pager, search, row actions.
4. **Form controls** (Input/Select/Textarea/DatePicker/FileUpload/OTP/Toggle/Checkbox/Radio) + Form (RHF+Zod) — for dedicated-page forms.
5. **Modal** (small-scope only), **Toast** provider, **Sheet**, **Tabs**.
6. **i18n** scaffold (i18next + en) + `AppProviders`.
Then **P1** (Firebase auth + RBAC + rules).

## Architecture / decisions
Unchanged from 00:31 checkpoint + `NEXLI_BUILD_PLAN.md` §14A. Key: free-tier (Spark, no Blaze, no Functions yet); RBAC via Firestore profile docs + rules; parent phone-OTP; students/parents can't change own password; major forms = dedicated pages; manual payments; in-app notifications; Leaflet/OSM maps; AI surfaces built but provider-less.

## Folder structure (Web/src)
```
app/ router.tsx FoundationPage.tsx (providers/ shell/ — to fill)
components/ Icon Avatar Badge Button Panel KPICard feedback AILockedOverlay charts index.ts
lib/ firebase.ts cn.ts format.ts hooks.ts legacy-charts.reference.js
styles/ nexli.css app.css
types/ roles.ts
features/ routes/ locales/en/ (to fill)
```

## Key notes
- Use `@/components` barrel + `cn()`.
- Charts already reduced-motion aware; LineChart draw-anim may need a layout-effect refinement in P9 (cosmetic).
- Firebase NOT yet imported by any rendered route → excluded from bundle; code-split when wired (P1).
- `.kpi-grid` (auto-fit) and `.grid.g-2/.g-3` + `.donut-wrap` etc. come from nexli.css.

## Known issues / TODO
- PWA manifest icons (192/512 png) not created (build warns only).
- Dev server not yet visually verified in browser.
- ESLint not configured (Prettier is).

## Next recommended actions
Build nav manifest → AppShell (sidebar/drawer/bottom-nav/topbar) → DataTable → form controls → modal/toast → i18n; verify build; then P1 auth/RBAC/rules. Update continuity files each milestone.
