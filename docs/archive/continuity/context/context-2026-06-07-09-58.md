# NEXLI — Context Checkpoint · 2026-06-07 09:58

> **Controlled context reset / handoff package generated.** Canonical recovery files are now:
> - `Web/context/LATEST_CONTEXT.md` (full current state)
> - `Web/resume/LATEST_RESUME_PROMPT.md` (paste into a new session)
>
> Rolling history preserved (00-31 → 09-58); newest = source of truth, but LATEST_* are canonical.

## State (summary — see LATEST_CONTEXT.md for full detail)
- **Phase: P0 (Foundation & UI Kit) ~90% — builds green.**
- Done: Vite/TS/PWA scaffold; Tailwind v4 theme + ported nexli.css + app.css; Firebase init (offline); **UI Kit** (Icon, Avatar, Badge, Button, Panel, KPICard, feedback, AILockedOverlay, charts, DataTable[table→cards]); **platform spine** (multi-tenant `lib/db.ts`, RBAC `lib/rbac.ts`, feature flags `lib/featureFlags.ts`, audit `lib/audit.ts`, `SessionProvider`); **navigation manifest** (`app/nav.ts`) + **AppShell** (sidebar/drawer/bottom-nav/appbar); ShellPreview demo; quality system (`docs/QUALITY_REVIEW.md`) + tracker (`context/MODULE_STATUS.md`).
- Architecture/decisions: unchanged — free-tier Spark (no Blaze/Functions yet), multi-tenant isolation, feature flags, audit, RBAC via Firestore profile docs, parent phone-OTP, students/parents can't change own password, major forms = dedicated pages, manual payments, in-app notifications, Leaflet/OSM maps, AI built-but-locked, rolling checkpoints, module quality gate.

## Next after restart
1. Finish P0 kit: form kit, Modal/Toast/Sheet/Tabs, i18n + AppProviders.
2. P1: auth (email/pass + phone OTP), route guards, session-driven AppShell container, deploy `firestore.rules` + indexes (needs `firebase login`).
3. Fan out modules to subagents (P2→P9) per CONVENTIONS, each through QUALITY_REVIEW.

## How to resume
Open a new Claude Code session and paste `Web/resume/LATEST_RESUME_PROMPT.md`. It will read LATEST_CONTEXT.md → spec → MODULE_STATUS.md → CONVENTIONS.md → QUALITY_REVIEW.md, confirm `cd Web && npm run build` is green, and continue from `MODULE_STATUS.md`.
