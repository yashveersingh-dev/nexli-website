# NEXLI — Context Checkpoint · 2026-06-07 01:00

> Rolling history kept (00-31, 00-43, 01-00). Newest = source of truth.

## Status
**P0 (Foundation) ~70% — builds green** (`cd Web && npm run build`; 86 modules; chunks split: app 10.85kB gz, firebase 130kB gz, react 60.8kB gz, router 30kB gz; CSS 9.73kB gz). **Platform spine complete.** Remaining P0: nav manifest, app shells, DataTable, form controls, modal/toast, i18n.

## Completed since 00:43 — the platform spine (cross-cutting, day-one)
- `types/models.ts` — School (tenant), Member, UserIndex, AcademicYear/Grade/Section, TenantRecord base.
- `lib/rbac.ts` — Permission catalog + ROLE_PERMISSIONS map (per role; '*' for admin roles) + `hasPermission`, `canManagePasswords`, `isPlatformRole`. (UI gating; rules are the real boundary.)
- `lib/featureFlags.ts` — **centralized feature flags**: FeatureFlagKey (ai, whatsapp, sms, email, online_payments, gps_tracking, advanced_analytics, hostel, transport, canteen, library, medical, alumni, smc, push), FEATURE_FLAGS metadata, DEFAULT_FLAGS, `resolveFlags(global, perSchool)` (DEFAULT◀global◀school; AI env override).
- `lib/db.ts` — **tenant-scoped Firestore layer**: platform refs (schools, subscriptions, plans, platform_settings, platform_announcements, platform_audit_log, feature_flags/global, userIndex) + tenant refs (`tenantCol/tenantDoc`, memberRef, schoolFlagsRef, tenantAuditCol) + real-time hooks `useDocument<T>`, `useCollection<T>`.
- `lib/audit.ts` — **platform-wide audit logging**: AuditAction union (user/student/fee/attendance/password/role/settings/etc., open-ended) + `writeAuditEvent` (append-only, tenant or platform log, never throws).
- `app/providers/SessionProvider.tsx` — unifies auth + tenant + role + permissions + flags. `useSession`, `useCan`, `useFlag`, `useAudit`. Resolves user via `/userIndex/{uid}` → super_admin (no school) or member+school+flags.
- `main.tsx` wraps app in `<SessionProvider>`.
- `vite.config.ts` — manualChunks (firebase/react/router/vendor) + chunkSizeWarningLimit.
- `docs/CONVENTIONS.md` — the contract for modules/subagents (spine APIs, kit, feature-module layout, DOD).

## Firestore data shape (decided)
- Tenant root/config: `schools/{schoolId}` (doc) + subcollections `schools/{schoolId}/{students,members,attendance,grades,fees,...,audit_log,settings}`.
- Per-school flags: `schools/{id}/settings/feature_flags`. Global flags: `feature_flags/global`.
- Platform-only: `subscriptions`, `plans`, `platform_settings/global`, `platform_announcements`, `platform_audit_log`, `userIndex/{uid}`.

## Architecture / decisions
Unchanged + reinforced: strict multi-tenant isolation (structural via db.ts + rules), centralized feature flags, day-one audit logging, free-tier (Spark/no Blaze/no Functions yet), RBAC via Firestore profile docs, parent phone-OTP, students/parents can't change own password, major forms = dedicated pages, manual payments, in-app notifications, Leaflet/OSM maps, AI built-but-locked. Subagents authorized for module fan-out after foundation. Rolling checkpoints kept.

## Folder structure (Web/src) — added
```
app/ router.tsx FoundationPage.tsx providers/SessionProvider.tsx
components/ Icon Avatar Badge Button Panel KPICard feedback AILockedOverlay charts index.ts
lib/ firebase.ts cn.ts format.ts hooks.ts rbac.ts featureFlags.ts db.ts audit.ts legacy-charts.reference.js
styles/ nexli.css app.css
types/ roles.ts models.ts
features/ routes/ locales/en/ (to fill)
docs/CONVENTIONS.md
```

## Known issues / TODO
- Firebase chunk 130kB gz (acceptable; later split firestore/auth + lazy-load).
- PWA icons (192/512 png) not created (warns only).
- Auth login UI + route guards not built yet (SessionProvider ready).
- Dev server not yet visually verified.
- ESLint not configured (Prettier is). `eslint-disable` comments are inert until ESLint added.

## Next recommended actions
1. **Nav manifest** `src/app/nav.ts` (NavItem type; role + flag filtered; AI-tagged).
2. **App shells** `src/app/shell/` — AppShell, Sidebar, Drawer, BottomNav (safe-area), TopBar, ContextSwitcher (mobile-first).
3. **DataTable** (table ≥768 / stacked cards <768) + Pager/Filters.
4. **Form controls** (Input/Select/Textarea/DatePicker/FileUpload/OTP/Toggle/Checkbox/Radio) + Form (RHF+Zod) for dedicated-page forms; **Modal** (small), **Toast** provider, **Sheet**, **Tabs**.
5. **i18n** scaffold (i18next + en).
6. **P1 auth:** login (email/pass + phone OTP) pages, route guards (status/role/flag), secondary-app provisioning, local Super Admin bootstrap script + first school.
7. Then **fan out modules to subagents** (per CONVENTIONS): SIS, attendance, fees, etc., each in `src/features/<module>/`; integrator registers routes/nav.
